<h1>Welcome to Lekker Plekke mate!</h1>

<table>
  <% @places.each do |place| %>
    <tr>
	<td><%= place.name %></td>
	 <td><%= link_to "Show", place_path(place) %></td>
    </tr>
  <% end %>
</table>

<%= link_to "New plekke", new_place_path %>